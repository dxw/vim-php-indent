  > :set expandtab shiftwidth=2 ft=php<CR>
  > :source indent/php.vim<CR>

  % <?php<CR>
  % <CR>
  % foreach ($x as $y) :<CR>
  % fn();<CR>
  % endforeach;<CR>
  % <CR>
  % fn();<CR>

  > gg=G

  <?php
  &
  foreach ($x as $y) :
    fn();
  endforeach;
  &
  fn();
